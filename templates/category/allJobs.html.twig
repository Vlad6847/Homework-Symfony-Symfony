{% extends 'base.html.twig' %}

{% block title %}
    {{ category.name }}
{% endblock %}

{% block body %}
    <h4>{{ category.name }}</h4>
    {% include 'table.html.twig' with {'category': category, 'jobs': activeJobs} only%}
    <div class="navigation text-center">
        {{ knp_pagination_render(activeJobs) }}
    </div>
{% endblock %}
