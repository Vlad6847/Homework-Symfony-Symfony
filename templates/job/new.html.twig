{% extends 'base.html.twig' %}

{% block title %}
    New Job
{% endblock %}

{% block body %}
    <h2>New Job</h2>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}

        <div class="form-group">
            <div class="col-md-offset-2 col-md-2">
                <input type="submit" class="btn btn-default" title="Submit"/>
            </div>
        </div>
    {{ form_end(form) }}
{% endblock %}
